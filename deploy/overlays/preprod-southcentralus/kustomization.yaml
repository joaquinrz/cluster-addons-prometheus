apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
- ../../base

patchesJson6902:
  - target:
      kind: Application
      name: prometheus-workload
    patch: |-
      - op: replace
        path: /metadata/name
        value: prometheus-workload-preprod-southcentralus
      - op: replace
        path: /spec/source/helm/parameters/1/value  # Assuming "clusterName" is the second parameter
        value: "preprod-southcentralus"
      - op: replace
        path: /spec/destination/server
        value: "https://aks-prepro-fleet-f22d63-tzfddg2s.hcp.southcentralus.azmk8s.io:443"
